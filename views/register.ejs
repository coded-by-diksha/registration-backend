<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title> Registration</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@500;700&display=swap');
        body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #2c3e50 0%, #8e44ad 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        .container {
            background: rgba(255,255,255,0.08);
            border-radius: 24px;
            box-shadow: 0 8px 32px 0 rgba(44,62,80,0.37);
            padding: 40px 32px;
            width: 350px;
            position: relative;
            z-index: 2;
            animation: fadeInUp 1.2s cubic-bezier(.68,-0.55,.27,1.55);
        }
        @keyframes fadeInUp {
            0% { transform: translateY(60px) scale(0.95); opacity: 0; }
            100% { transform: translateY(0) scale(1); opacity: 1; }
        }
        .container::before {
            content: '';
            position: absolute;
            top: -30px; left: -30px; right: -30px; bottom: -30px;
            background: linear-gradient(120deg, #f5e6ff 0%, #d1c4e9 100%);
            opacity: 0.15;
            border-radius: 32px;
            z-index: -1;
            filter: blur(8px);
            animation: pulse 3s infinite alternate;
        }
        @keyframes pulse {
            0% { opacity: 0.15; }
            100% { opacity: 0.25; }
        }
        h2 {
            text-align: center;
            color: #fff;
            font-size: 2rem;
            margin-bottom: 24px;
            letter-spacing: 2px;
            text-shadow: 0 2px 8px #8e44ad88;
            animation: fadeIn 1.5s;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 22px;
        }
        .input-group {
            position: relative;
            animation: inputFadeIn 1.2s;
        }
        @keyframes inputFadeIn {
            0% { opacity: 0; transform: translateY(20px);}
            100% { opacity: 1; transform: translateY(0);}
        }
        label {
            position: absolute;
            left: 18px;
            top: 14px;
            color: #b39ddb;
            font-size: 1rem;
            pointer-events: none;
            transition: 0.3s;
            background: transparent;
            padding: 0 4px;
        }
        input:focus + label,
        input:not(:placeholder-shown) + label {
            top: -12px;
            left: 12px;
            font-size: 0.85rem;
            color: #8e44ad;
            background: #fff;
            border-radius: 6px;
            padding: 0 6px;
            box-shadow: 0 2px 8px #8e44ad22;
        }
        input {
            width: 100%;
            padding: 16px 18px;
            border: none;
            border-radius: 12px;
            background: rgba(255,255,255,0.18);
            color: #2c3e50;
            font-size: 1rem;
            outline: none;
            transition: background 0.3s, box-shadow 0.3s;
            box-shadow: 0 2px 8px #8e44ad22;
            margin-bottom: 0;
        }
        input:focus {
            background: rgba(255,255,255,0.28);
            box-shadow: 0 4px 16px #8e44ad44;
        }
        .btn {
            background: linear-gradient(90deg, #8e44ad 0%, #6c3483 100%);
            color: #fff;
            border: none;
            border-radius: 12px;
            padding: 14px 0;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 4px 16px #8e44ad44;
            transition: background 0.3s, transform 0.2s;
            margin-top: 12px;
            letter-spacing: 1px;
            animation: btnPop 1.5s;
        }
        @keyframes btnPop {
            0% { transform: scale(0.9); opacity: 0;}
            100% { transform: scale(1); opacity: 1;}
        }
        .btn:hover {
            background: linear-gradient(90deg, #6c3483 0%, #8e44ad 100%);
            transform: scale(1.04);
        }
        .glow {
            position: absolute;
            top: -60px; left: -60px;
            width: 180px; height: 180px;
            background: radial-gradient(circle, #8e44ad88 0%, transparent 70%);
            z-index: 0;
            animation: glowMove 6s infinite alternate;
            pointer-events: none;
        }
        @keyframes glowMove {
            0% { top: -60px; left: -60px;}
            100% { top: 60px; left: 60px;}
        }
        .footer {
            text-align: center;
            color: #b39ddb;
            font-size: 0.95rem;
            margin-top: 18px;
            opacity: 0.8;
            animation: fadeIn 2s;
        }
    </style>
    

<script>
        function validateForm() {
            const username = document.getElementById('username').value.trim();
            const email = document.getElementById('email').value.trim();
            const conpassword = document.getElementById('conpassword').value.trim();
            const password = document.getElementById('password').value.trim();
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

            if (password !== conpassword) {
                alert('Passwords do not match.');
                return false;
            }
            if (username.length < 3) {
                alert('Username must be at least 3 characters long.');
                return false;
            }
            if (!emailPattern.test(email)) {
                alert('Please enter a valid email address.');
                return false;
            }
            if (password.length < 6) {
                alert('Password must be at least 6 characters long.');
                return false;
            }
         if(username === "" || email === "" || password === "" || conpassword === ""){
                alert('All fields are required.');
                return false;
            }
            if(username.length > 20){
                alert('Username should not exceed 20 characters.');
                return false;
            }

            if (username.length>3 || password.length>6 || emailPattern.test(email)) {
                alert('Registration successful!');
            }
            return true;

        }
    </script>
</head>
<body>
    <div class="glow"></div>
    <div class  ="container">
        <h2> Registration</h2>
        <form action="/register" method="POST" autocomplete="off" onsubmit="return validateForm()">
            <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                <div style="color: #d8000c; background: #ffeaea; padding: 10px; border-radius: 8px; margin-bottom: 16px;">
                    <% errors.forEach(function(error) { %>
                        <div><%= error.msg %></div>
                    <% }); %>
                </div>
            <% } %>
            <div class="input-group">
                <input type="text" name="username" id="username" required placeholder=" " value="<%= oldInput && oldInput.username ? oldInput.username : '' %>" />
                <label for="username">Username</label>
            </div>
            <div class="input-group">
                <input type="email" name="email" id="email" required placeholder=" " value="<%= oldInput && oldInput.email ? oldInput.email : '' %>" />
                <label for="email">Email</label>
            </div>
            <div class="input-group">
                <input type="password" name="password" id="password" required placeholder="" />
                <label for="password">Password</label>
            </div>
            <div class="input-group">
                <input type="password" name="conpassword" id="conpassword" required placeholder=" " />
                <label for="conpassword">Confirm Password</label>
            </div>
            <button class="btn" type="submit">Register</button>
        </form>
        <div class="footer">
            Already have an account? <a href="/login" style="color:#8e44ad;text-decoration:underline;">Login</a>
        </div>
    </div>
</body>
</html></div></html>